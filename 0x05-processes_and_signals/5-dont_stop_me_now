#!/usr/bin/env bash

# Read the process ID from the file
pid=$(cat /tmp/my_process.pid 2>/dev/null)

if [ -n "$pid" ]; then
    # Kill the process using SIGTERM
    kill -15 "$pid"
    echo "Termination signal sent to process $pid"
else
    echo "Could not find the process ID"
fi
